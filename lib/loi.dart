import 'package:flutter/material.dart';
import 'package:wed/santa.dart';

class Loibi extends StatelessWidget {
  const Loibi({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Santa();
  }
}
